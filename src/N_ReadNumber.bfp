//Reads a number, stored as a string over the next n cells
//Cells: 0,1, 2, 3, 4

+ /so loop starts
[
    ,
    n{}{
        b /remove input if not mueric
    }
    /b makes cell 0
    >[-]< /reset [1], pointer at [0]
    [
        *48-
        >
    ] /if value is not 0 (therefore numeric), change to integer value, move to next cell.
    + /continue loop. it will be overwritten with the write input
]